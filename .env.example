# Burakeye administrator account. Set real values only in your hosting provider's environment settings.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=choose-a-long-unique-password

# Optional locally. On Render, mount a persistent disk at this path.
STORAGE_DIR=storage
